---
description: Build a new assistant (agent) for a task that needs judgment
---

The user wants a new assistant. Follow `docs/builder/build-guide.md`, treating this as an **agent**
build (but confirm agent-vs-skill in Phase 0 — if it's really a fixed procedure, switch to a skill).

Work through the five phases conversationally and in plain language. When you build, write the
assistant to `agents/<name>.md` in the exact format from the build guide, then register it in
`docs/catalog.md` and run the self-heal check.

What the user wants the assistant to do: $ARGUMENTS
