# AI Desk

AI Desk is **harness-agnostic** — it runs in any AI coding tool. To avoid drift, all instructions
live in one place: **[`AGENTS.md`](AGENTS.md)**.

**Claude Code:** read `AGENTS.md` now and follow it as your project instructions. The import below
pulls it in automatically; if for any reason it isn't loaded, open `AGENTS.md` and follow it.

@AGENTS.md
